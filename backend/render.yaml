services:
  - type: web
    name: chat-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: APP_CORS_ALLOWED_ORIGINS
        value: http://localhost:5173,https://omnexa-flame.vercel.app
