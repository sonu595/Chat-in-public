# Application
spring.application.name=chat
server.port=${PORT:8080}
server.address=0.0.0.0
spring.main.banner-mode=off

# JWT Configuration
jwt.secret=${JWT_SECRET:mysecreolwberljwbljbgmustbe32charslong}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Redis Configuration - Upstash
spring.data.redis.url=rediss://default:gQAAAAAAAWryAAIgcDIxNzc4NDFmZjUxNDI0NTVlOGVjOTVjZWFmY2EwZWFlOQ@patient-horse-92914.upstash.io:6379
spring.data.redis.timeout=10000ms
spring.data.redis.connect-timeout=10000ms
spring.data.redis.ssl.enabled=true

# Email Configuration (use environment variables in production)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:sonushekhawat2077@gmail.com}
spring.mail.password=${MAIL_PASSWORD:clon svsk wnjm ewbk}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# PostgreSQL Database
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/Chatdb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:heroic}
spring.jpa.hibernate.ddl-auto=create-drop

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

profile.avatar.max-size=5MB
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
